runtime: nodejs20
env: standard

instance_class: F1

automatic_scaling:
  max_instances: 1

handlers:
  - url: /
    static_files: index.html
    upload: index.html
  - url: /(.*)
    static_files: \1
    upload: (.*)
